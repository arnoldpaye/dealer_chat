import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

import { Button } from 'react-bootstrap';

ReactDOM.render(<Button bsStyle="primary">Primary button</Button>, document.getElementById('root'));
